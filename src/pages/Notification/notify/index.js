import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const index = (props) => {
  return (
    <View>
      <Text>{props.namabarang}</Text>
      <Text></Text>
    </View>
  );
};

export default index;

const styles = StyleSheet.create({});